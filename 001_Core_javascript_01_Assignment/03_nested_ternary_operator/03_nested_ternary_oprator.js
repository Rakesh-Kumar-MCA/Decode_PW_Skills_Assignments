// Use a nested ternary operator 

const num = 1; // to assign 1 value to num variable

let result = num > 0 ? 'Positive' : num < 0 ? 'Negative' : 'Zero' // Nested Tarnary Oprator

console.log(result) // result :  Positive

