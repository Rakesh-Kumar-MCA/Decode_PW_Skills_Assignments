// building a weather application with the help of ternary oprator 

let currentTemperator = 30; 

let weatherTemp = currentTemperator >= 30 ? 'Hot' : 'Moderate' // check candition

console.log(`Weather condition is ${weatherTemp} because current temperator is ${currentTemperator}°C` )